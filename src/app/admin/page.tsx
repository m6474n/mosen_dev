import AdminDashboardView from '@/components/AdminDashboardView';

export const metadata = {
  title: 'Admin Dashboard — Mosen CMS',
};

export default function AdminPage() {
  return <AdminDashboardView />;
}
